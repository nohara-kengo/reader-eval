# 開発用イメージ（docker compose の web から使用）。ホットリロード前提。
FROM node:22-alpine

WORKDIR /app

# 依存だけ先に入れてキャッシュを効かせる（ソースは compose の bind mount で供給）
COPY package.json package-lock.json ./
RUN npm ci

EXPOSE 3000
CMD ["npm", "run", "dev"]
